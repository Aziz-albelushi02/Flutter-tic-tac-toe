// adding colors to the app 

import 'package:flutter/animation.dart';
import 'package:flutter/material.dart';

class MainColor{
  static Color primaryColor = Color(0xFF00061a);
  static Color SecondAnyColor = Color(0xFF001456);
  static Color acccentColor = Color(0xFF4169e8);
}